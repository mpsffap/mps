---
- name: Essentials OS
  ansible.builtin.apt:
    name:
      - curl
      - wget
      - gnupg
      - vim
      - lsb-release
      - debconf
      - debconf-utils
      - fontconfig
      - psutils
      - psmisc
      - htop
    state: present
  become: true
